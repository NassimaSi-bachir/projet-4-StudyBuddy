<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue sur Study Buddy{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <link rel="shortcut icon" href="/images/studybuddy-logo.png">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Patrick+Hand&family=Patrick+Hand+SC&family=Permanent+Marker&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/css/style-studybuddy.css">
        {% block stylesheets %}
        {% endblock %}

        
    </head> 

    <body> 

<nav class="navbar navbar-expand-xl navbar-light text-center">
    <div class="container-fluid d-flex align-items-center">
        <a class="navbar-brand d-md-none" href="#">
            <img src="/images/studybuddy-logo.png" alt="logo study buddy">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav d-flex mx-auto align-items-center justify-content-around w-100">
              <li class="nav-item mx-3">
                <a class="nav-link d-flex flex-column align-items-center" aria-current="page" href="{{path('app_video') }}">Accueil <i data-feather="home"></i></a>
            </li>
            <li class="nav-item mx-3">
              <a class="nav-link d-flex flex-column align-items-center" href="{{path('app_category') }}">Catégories <i data-feather="search"></i></a>
            </li>
            <a class="navbar-brand mx-5" href="{{path('app_home') }}">
              <img src="/images/studybuddy-logo.png" alt="logo study buddy">
              <p>Study Buddy</p>
            </a>
            <li class="nav-item mx-3">
              <a class="nav-link d-flex flex-column align-items-center" href="#">Favoris <i data-feather="heart"></i></a>
            </li>
            <li class="nav-item mx-3">
              {% if app.user is not null %}
              <!-- à changer pour mettre le lien vers le profil -->
              <a class="nav-link d-flex flex-column align-items-center" href="{{ path('app_login')}}">Profil <i data-feather="user"></i></a>
              {% else %}
              <a class="nav-link d-flex flex-column align-items-center" href="{{ path('app_login')}}">Connexion <i data-feather="user"></i></a>
              {% endif %}
            </li>
          </ul>
        </div>
    </div>
</nav> 




<main class="container-lg py-4">
        {% block body %}{% endblock %}
</main>
          <script src="https://unpkg.com/feather-icons"></script> 

        {% block javascripts %}
        {% endblock %}
        <script>feather.replace()</script>
           <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>


    </body>
</html>

{% extends 'base-backend.html.twig' %}

{% block title %}Modifier les vues{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_view_index') }}" class="btn btn-dark mb-3">Retour</a>

    <div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modifier les vues</h1>
        {{ include('admin_view/_delete_form.html.twig') }}
    </div>

    {{ include('admin_view/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}
